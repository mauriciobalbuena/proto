document.getElementById("start-event-btn").addEventListener("click", () => {
    // Aquí podrías hacer una llamada a la API si fuera necesario
    // Por ahora solo mostramos un mensaje simulado
    const mensaje = document.getElementById("start-message");
    mensaje.textContent = "¡Evento iniciado correctamente!";
  });
  